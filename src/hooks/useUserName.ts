// import { useMemo } from 'react'
// import { isWideScreen } from '../helpers/sizes'

export const useUserName = (userName: string): string => {
  //   const name = useMemo(
  //     () =>
  //     //   isWideScreen
  //     //     ? userName
  //         : userName
  //             .split(' ')
  //             .map(el => el[0])
  //             .join(''),
  //     [userName, isWideScreen],
  //   )

  return userName
}
